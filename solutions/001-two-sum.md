# 1. 两数之和

## 题目描述

给定一个整数数组 `nums` 和一个整数目标值 `target`，请你在该数组中找出 **和为目标值 `target` 的那 **两个** 整数，并返回它们的数组下标。

你可以假设每种输入只会对应一个答案。但是，数组中同一个元素在答案里不能重复出现。

你可以按任意顺序返回答案。

**示例 1：
```
输入：nums = [2,7,11,15], target = 9
输出：[0,1]
解释：因为 nums[0] + nums[1] == 9 ，返回 [0, 1] 。
```

**示例 2：**
```
输入：nums = [3,2,4], target = 6
输出：[1,2]
```

**示例 3：**
```
输入：nums = [3,3], target = 6
输出：[0,1]
```

**提示：**
- `2 <= nums.length <= 10^4`
- `-10^9 <= nums[i] <= 10^9`
- `-10^9 <= target <= 10^9`
- **只会存在一个有效答案**

---

## 解题思路

### 方法一：暴力枚举

**思路：**
遍历数组中的每个元素 `x`，然后查找数组中是否存在另一个元素 `target - x`。

**步骤：
1. 使用两层循环，外层循环遍历数组中的每个元素
2. 内层循环在剩余元素中查找是否存在 `target - nums[i]`
3. 找到后返回两个下标

**代码实现：**

```python
def twoSum(nums, target):
    n = len(nums)
    for i in range(n):
        for j in range(i + 1, n):
            if nums[i] + nums[j] == target:
                return [i, j]
    return []
```

**复杂度分析：**
- 时间复杂度：O(n²)，其中 n 是数组中的元素数量。最坏情况下数组中任意两个数都要被匹配一次。
- 空间复杂度：O(1)。

---

### 方法二：哈希表（推荐）

**思路：**
使用哈希表来存储已经遍历过的元素及其下标，这样可以在 O(1) 的时间复杂度内查找目标元素。

**步骤：**
1. 创建一个哈希表，用于存储元素值到下标的映射
2. 遍历数组，对于每个元素 `nums[i]`：
   - 计算需要查找的目标值 `complement = target - nums[i]`
   - 检查哈希表中是否存在 `complement`
   - 如果存在，直接返回 `[哈希表[complement], i]`
   - 如果不存在，将 `nums[i]` 和其下标 `i` 存入哈希表
3. 遍历结束后如果没有找到，返回空数组

**代码实现：**

```python
def twoSum(nums, target):
    num_map = {}
    
    for i, num in enumerate(nums):
        complement = target - num
        if complement in num_map:
            return [num_map[complement], i]
        num_map[num] = i
    
    return []
```

**复杂度分析：**
- 时间复杂度：O(n)，其中 n 是数组中的元素数量。对于每一个元素 x，我们可以 O(1) 地寻找 target - x。
- 空间复杂度：O(n)，其中 n 是数组中的元素数量。主要为哈希表的开销。

---

## 解题要点

1. **哈希表的优势：
   - 查找时间复杂度从 O(n) 降低到 O(1)
   - 通过空间换时间的典型应用

2. **为什么可以边遍历边存储：
   - 不需要先构建哈希表再查找，而是在遍历过程中同时查找
   - 这样可以避免使用同一个元素两次（因为查找的是之前的元素）

3. **注意事项：
   - 数组中可能存在重复元素的情况（如示例3）
   - 同一个元素不能重复使用
   - 返回的是下标，不是值

---

## 测试用例

| 测试用例 1：
```python
nums = [2, 7, 11, 15]
target = 9
输出: [0, 1]
```

测试用例 2：
```python
nums = [3, 2, 4]
target = 6
输出: [1, 2]
```

测试用例 3：
```python
nums = [3, 3]
target = 6
输出: [0, 1]
```

测试用例 4（负数）：
```python
nums = [-1, -2, -3, -4, -5]
target = -8
输出: [2, 4]
```

---

## 相关题目

- [15. 三数之和](https://leetcode.cn/problems/3sum/)
- [18. 四数之和](https://leetcode.cn/problems/4sum/)
- [167. 两数之和 II - 输入有序数组](https://leetcode.cn/problems/two-sum-ii-input-array-is-sorted/)
- [653. 两数之和 IV - 输入二叉搜索树](https://leetcode.cn/problems/two-sum-iv-input-is-a-bst/)

---

## 总结

两数之和是 LeetCode 的入门题目，也是哈希表应用的经典案例。通过这个题目，我们可以深刻理解了**空间换时间**的思想，以及哈希表在查找操作的高效性。

**关键点：
1. 暴力解法时间复杂度高，但空间复杂度低
2. 哈希表解法时间复杂度低，但空间复杂度高
3. 根据实际场景选择合适的解法

---

*最后更新：2026-04-20